import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.swuteam.spacenavigator',
  appName: 'space-navigator',
  webDir: 'build'
};

export default config;
